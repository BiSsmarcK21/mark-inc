# client/.env.example
# -----------------------------------------------------------
# 💻 Пример файла окружения для frontend (Next.js)
# -----------------------------------------------------------
# Этот файл используется только как шаблон.
# Реальные значения указываются в client/.env (не коммитится).
# -----------------------------------------------------------

# Базовый адрес API, на который будут идти запросы из браузера.
# В DEV указываем localhost (идёт через Nginx → Express).
# В PROD — реальный домен, например: https://mark-inc.ru/api
NEXT_PUBLIC_API_URL=http://localhost/api

# Среда выполнения (development / production)
NODE_ENV=development
