# server/.env.example
# -----------------------------------------------------------
# 🧠 Пример файла окружения для backend (Express + Prisma)
# -----------------------------------------------------------

# Подключение к PostgreSQL (пример формата)
# Пример: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@db:5432/mark_db?schema=public"

# Порт, на котором запустится сервер
PORT=4000

# Среда (development / production)
NODE_ENV=development
